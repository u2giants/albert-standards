# Ansible

Host-layer Infrastructure-as-Code for the Hetzner server.

- [ANSIBLE-IMPLEMENTATION-PLAN.md](ANSIBLE-IMPLEMENTATION-PLAN.md) — the full, self-contained plan a fresh AI session can execute to build the host-Ansible project + GitHub Actions apply pipeline.
